import styled from 'styled-components';
import MovieInfoWithData from '../../components_apollo/MovieInfoWithData';

const Movie = styled.div`

`

export default () => (
  <Movie id="index">
    <MovieInfoWithData />
  </Movie>
);

